# Spring_Boot_RESTFul_MySQL_CRUD
 Spring Boot Security REST + JPA + Hibernate + MySQL CRUD
 
 
 mvn clean install
 mvn spring-boot:run 
open  http://localhost:8080/user/articles
as username eric/m123 with ADMIN 










*** REFconcretepage
